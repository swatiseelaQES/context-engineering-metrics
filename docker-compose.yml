services:
  context-engineering-metrics:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - .:/app
    command: bash -lc "pip install -r requirements.txt && python experiments/full_system_comparison.py"
