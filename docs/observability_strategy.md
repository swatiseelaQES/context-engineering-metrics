# Observability Strategy

Every agent returns:

- output
- retrieved context
- tools called
- retry count
- latency
- token usage

These fields are enough to build basic AI workflow observability before integrating full tracing platforms.
